
export class Post {
    number: string;
    title: string;
    description: string;

    constructor (number: string, title: string, description: string) {
      this.number = number;
      this.title = title;
      this.description = description
    }
}